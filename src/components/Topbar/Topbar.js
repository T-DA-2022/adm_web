import React from "react";

import { TopbarContainer } from "./Topbar.element";

const Topbar = () => {
  return (
    <TopbarContainer>
      <div>Topbar</div>
    </TopbarContainer>
  );
};

export default Topbar;
